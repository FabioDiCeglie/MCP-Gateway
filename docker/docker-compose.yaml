services:
  gateway:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ../src:/app/src:ro
      - ../gateway.yaml:/app/gateway.yaml:ro
